import { useContext, useEffect } from "react";
import { View } from "react-native";
import { AppContext } from "../Contexts/App";

export default function TransitionUpdate(): React.JSX.Element {

    return (
        <View></View>
    )
}